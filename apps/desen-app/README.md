# Desen App

The visual authoring and publishing product intended for `desen.app`. It will consume editor-web,
runtime-react, and the reference catalog through public package APIs. Desen App is one product
built on DESEN; the protocol and App-independent developer ecosystem live separately on the
DESEN Developer Platform at `desen.run`.

## Status

Structure reserved. Implementation begins only at its tracked milestone gate.
