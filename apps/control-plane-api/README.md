# Control Plane API

A small local-first service for editable sources, immutable bundles, and mutable channel pointers.
Publication logic stays in the pure publisher package. This is implementation infrastructure for
the proof environment, not the public `desen.run` developer API.

## Status

Structure reserved. Implementation begins only at its tracked milestone gate.
