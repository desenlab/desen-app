import { privateHostDetail } from "../desen-app/dev/local-operation-private.mjs";

/** A neighboring dev module must not inherit the reviewed listener's exception. */
export const forbiddenDevModuleComposition = privateHostDetail;
