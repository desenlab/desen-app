{
  "name": "@desen/design-system-release",
  "version": "0.0.0",
  "private": true,
  "description": "Platform-neutral immutable design-system release identity and store contracts for DESEN.",
  "license": "Apache-2.0",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src test --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:public-package": "tsc -p tsconfig.build.json && tsc -p tsconfig.public-package.json --noEmit && node --test test/public-package.mjs"
  },
  "dependencies": {
    "@desen/design-system-core": "workspace:*",
    "@desen/protocol": "workspace:*"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
