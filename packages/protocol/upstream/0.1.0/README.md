# Frozen upstream baseline

This directory records the exact DESEN 0.1.0 protocol baseline. M02-T01 will vendor the normative
schemas, examples, vectors, and digest helper as checksum-verified read-only inputs.

Do not edit vendored upstream files. Any implementation discovery belongs in
`docs/plan/PROTOCOL-FINDINGS.md`.
