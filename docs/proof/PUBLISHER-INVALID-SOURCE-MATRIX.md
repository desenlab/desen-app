# M06-T11 — Public invalid-Source no-Bundle matrix

M06-T11 is `PASS` for the task-owned invalid-publication matrix and closes the deterministic
Publisher milestone only after the final artifact pin below is generated.

## Proven boundary

An isolated process imports the built public `packages/publisher/dist/index.js` package root. Every
publication case crosses only its two-argument `publishDesenSource` operation; separate public
registry assertions call `isPublisherDiagnosticCode` and `getPublisherDiagnosticDefinition`.
All 127 reviewed invalid raw Source, Source contract, Catalog observation, capability contract,
execution contract, and reachable finite-limit vector stops at its exact earliest public pipeline
stage. Every rejection is a recursively immutable exact `{ diagnostics, ok, stage }` shell with a
nonempty error-first report; the first diagnostic stage equals the result stage and no Bundle or
intermediate publication authority is exposed.

The matrix authenticates `PIPE-025` through `PIPE-034`, plus the naturally reachable
normalization and final-Bundle limits at `PIPE-037` and `PIPE-039`. Simultaneous stage-eight,
stage-nine, and stage-ten defects prove earliest-stage precedence. A deprecation warning survives
only a complete success, uses fixed sanitized text, and is suppressed when a later blocking stage
rejects publication. Dynamic runtime obligations remain a valid successful publication at the
exact public ceiling rather than becoming an invented static error.

Beyond the already covered raw Source, Catalog, structural-report, obligation-report,
Source-trace-count, normalization, and Bundle limits, the final audit adds 12 independent closure
vectors. They separately cross inherited parse and Catalog report budgets, capability error-report
pointer and aggregate budgets, deprecation-warning count, pointer, and aggregate budgets,
execution-report count, pointer, and aggregate budgets, plus Source-preservation pointer and
aggregate-trace budgets while neighboring limits remain admissible. This prevents one broad
count-limit test from standing in for independently enforced public budgets. An independent
production-source branch inventory found no remaining naturally reachable default-limit branch at
the public two-argument boundary.

Five additional reviewed branch vectors distinguish behavior and resource traversal categories,
Source document and requirement identities, and selected-package versus inner-Catalog identity.

## Authorities and hostile boundaries

The proof pins exactly the M06-T03 through M06-T10 artifacts, the frozen trace rows, the exact
focused Publisher case table and test names, the built public Publisher files, and the public
finite Source JSON profile. As the one-way successor authority, it also pins the exact current
hashes and T11 registration semantics of the workspace and Publisher manifests, single-pass
quality-gate source and test, and the updated M06-T08 through M06-T10 proof readers and hostile
successor tests. It additionally authenticates the current M06-T02 compatibility reader that keeps
the original task-time Catalog-resolution receipt immutable while auditing the documented successor
source and declaration. Those historical artifacts do not depend back on T11. Evidence inputs use
bounded inert byte snapshots, proxy rejection, fatal UTF-8 decoding, and no-follow regular-file
reads. The generator accepts no semantic override and commits deterministic bytes through the
shared same-directory atomic writer.

The receipt also classifies exactly 31 frozen conformance, pipeline, prose, and diagnostic rows for
this task. `PIPE-035`, `PIPE-036`, `PIPE-038`, and `PIPE-040` retain their exact successful
prerequisite proofs; `PIPE-041` remains a `JUSTIFIED_NA` unsigned-publication non-claim. PF-047 is
recorded only as the task-local `A-011` and `D-009` slice, with neither a complete-rule claim nor a
frozen-ledger reassignment.

`docs/proof/artifacts/publisher-0.1.0-invalid-source-matrix.json`

`sha256:a05937fe698b6922fae01fe059f12fe1a83d77facdfd24a59d31a8ed7835b897`

## Explicit non-claims

The finite table closes the reviewed naturally reachable public branch and trace matrix with
representative malformed values; it does not enumerate every conceivable invalid JSON or Source
value.

No fake public negative is manufactured for `source-digest`, `authoring-removal`,
`catalog-pinning`, or `bundle-revision`. Once their authenticated predecessors succeed, those
deterministic stages do not have a natural invalid data input at the public two-argument boundary.
Their successful behavior remains authenticated by the pinned M06-T07 through M06-T10 evidence.
The successor compatibility files are current-hash authorities in T11; their historical receipts
are not rewritten and no dependency cycle is introduced.

The matrix proves atomic no-Bundle publication rejection. It does not prove signing, storage,
activation, deployment, runtime execution, host, adapter, editor, network, or control-plane
behavior.

## Reproduction after the final pin

```sh
pnpm --filter @desen/publisher... build
pnpm --filter @desen/publisher test:invalid-source-matrix
node scripts/generate-publisher-invalid-source-matrix-proof.mjs
node scripts/verify-publisher-invalid-source-matrix.mjs
node --test tests/publisher-invalid-source-matrix.test.mjs
```
