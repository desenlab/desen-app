# Desen App evergreen product composition

Task: M10-T01C

Status: DONE

P-08: PROVEN

P-09: PARTIAL

Predecessor artifact: `sha256:cd7366014a0cb6f056fa78392f81ef7cb4b5be2f523b95e5984c704be3caf0e8`

Final artifact: `sha256:779434ca834b8d770c726d905408f0a3d0a7145abbc6eaf2b81f1e77466b46ac`

## Scope

M10-T01C replaces reference-product defaults in reusable Desen App composition with one exact,
factory-authenticated workspace profile. The trusted composition root now selects project and
surface metadata, independent route and Source identities, storage identity, the complete admitted
Catalog set and package candidates, adapter registry, runtime ports, token CSS values, and optional
publication binding. DESEN data still selects no executable code.

One shared exact document-admission boundary binds direct App embedding, persistence, and
publication to the same profile-owned document id, entry, complete surface inventory, complete
Catalog requirement set, and interaction validation. A fixed publication adapter is authenticated
by both profile-owned channel and host before either effect callback can run. Non-entry canvas
selection remains a transient preview only: Save retains the authored entry and Publish emits the
base Source revision and Bundle bytes rather than the transient canvas revision.

Generic authoring, preview, runtime, persistence, and publication paths consume this admitted
profile. The reference sign-in product remains available only as an explicit composition passed by
`main.tsx`; it is no longer an implicit default in reusable modules. Exact own-data capture,
Catalog/package bijection, Source validation, registry coverage, route mapping, forged handles,
wrong surfaces, and absent publication authority fail closed.

Normal project inventory comes only from the authenticated workspace profile. Optional gallery
fixtures cross a separate opaque WeakMap-backed inert-inventory handle and cannot be composed with
Source, mutation, persistence, publication, or project-creation authority. Synthetic Run replaces
all nine profile host-port families—navigation, storage, product operations, resources, tokens,
context, environment, clock, and diagnostics—with inert bounded implementations; only the
explicit Catalog fixture operation controller remains callable.

Focused tests admit a two-surface feedback project backed by two Catalogs, preserve route slugs that
differ from Source surface identities, reject incomplete package sets and forged registries, and
render a non-auth feedback surface through the complete application. A source-level residue guard
rejects reintroduction of account/sign-in example authority into generic composition modules.
Dedicated application, inventory-fixture, operation-fixture, preview, and publication regressions
bind opaque handle identity, controller revocation, hostile/accessor rejection, complete host-port
isolation, and transient-versus-base Save/Publish behavior.

## Direct authority and execution

The deterministic artifact authenticates the immutable M10-T01B predecessor and exact receipts for
the workspace-profile and inert-inventory factories, explicit reference authoring/product/fixture
compositions, generic App/runtime/persistence/publication consumers, multi-Catalog helpers, focused
positive and negative tests, package scripts, and passive proof entrypoints. It binds 64 exact
tracked receipts and 10 independent root mutation cases.

Reviewed reader checkpoint sequence 66 preserves all 65 predecessor checkpoints, adds this exact
19,299-byte artifact, reseals current reader indexes `[70..97, 102, 103, 104, 105]`, and appends
the T01C proof/root readers at `[106, 107]`. Its exact head is
`sha256:3bf2c27ca51f8ab6751dd0d026bbbf461ac2c6acea6fcc3088f7d011ae96fb83`, covering 54 frozen
artifacts and 108 current readers. The 89/89 checkpoint suite and 242/242 historical App reader
cases are local authority only; hosted exact-head success remains separate.

Historical readers authenticate the exact M10-T01C artifact first, then consume one separately
pinned, bounded gzip bridge containing only the exact M10-T01B task-time files and frozen
compatibility projections required by their existing mutation suites. The committed bridge is
authenticated by compressed byte length and SHA-256 before bounded decompression; its canonical
JSON identity, base commit, dense path inventory, canonical base64 entries, projection set, and
successor-added path set are then checked without Git, network, or product execution. Caller
overrides replace only their exact target bytes, so historical policy mutations keep their original
error codes while unrelated successor drift remains authorized only by M10-T01C receipts.

The bridge is reproducible from an exact detached `a44575d48e073468da6b25eb8b31a375218caf0a`
worktree with its pinned dependencies built. The destination must not already exist:

```bash
node scripts/generate-desen-app-t01b-historical-reader-bridge.mjs \
  /path/to/exact-t01b-worktree \
  /path/to/desen-app-0.1.0-t01b-historical-reader-bridge.json.gz
```

```bash
pnpm --filter @desen/app-web exec vitest run test/project-workspace-profile.test.ts test/evergreen-product-composition.test.tsx
node scripts/verify-desen-app-evergreen-product-composition.mjs
node --test tests/desen-app-evergreen-product-composition.test.mjs
```

## Explicit nonclaims

This evidence does not admit profiles from untrusted documents and does not permit arbitrary
executable selection. It does not prove every product schema or workflow. M10-T02 through M10-T04,
production integration, remote deployment, multi-user persistence, and G10 closure remain outside
this task. The deterministic reader starts no product server, browser, listener, or external host;
a local artifact does not imply hosted exact-head success.
