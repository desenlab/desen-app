# Desen App invalid publication

Task: M10-T06

Status: DONE

M10-T05 parent: `sha256:80c0b815a813ef462233b48a7fffe7c4d0bbf391aefc68eb9a6174da6bd84bd3`

M09-T13 parent: `sha256:8ac4d81d9097e188860757c637673ff406ba9f82b8cd8f379f184ef85138e972`

Final artifact: `sha256:a28bf2b6eec77002a1018bbf872bf9e497d41e78c5de2d912dc873548d63a58d`

The task-time artifact is a local completion candidate. Merge and final closure remain conditional
on the exact current pull-request head and fresh merged-main hosted quality and browser runs;
this header does not claim that those runs have already passed.

## Product boundary

The optional Source & release → Advanced Source workflow reviews detached text through the real
public Publisher and continuous editor validator. Invalid component properties, events, and slots
produce exact public codes and pointers plus node-linked diagnostics. Rejection does not replace
the valid authoring document or canvas, write Source or Bundle data, update the channel, or request
host activation. A repair becomes a local document only; ordinary Save source → Publish is still
required. The visual designer journey does not require JSON.

## Independent evidence

- Product tests cover bounded input, valid repair, the three invalid categories, exact workspace
  identity, stale baselines, pending operations, genuine existing layer links, and discard.
- The deterministic reader performs fresh public Publisher/validator calls and complete independent
  App/reference-host build-graph audits. It checks source and test-declaration authority but does
  not represent declaration inspection as executed tests or Chromium success.
- The real Chromium journey begins with a visibly authored and activated baseline, rejects all
  three categories through the product UI, checks exact node diagnostics and zero external writes,
  then repairs and publishes each candidate. The active revision and generation remain unchanged
  during rejection; the served host static bytes remain unchanged throughout.
- The immutable T05 artifact is preserved byte for byte. Current T06 graph and source receipts are
  authenticated separately; historical graph results are never relabeled as current observations.

## Local verification receipt

The 193,291-byte artifact records 32 tracked authority receipts, ten root test cases, ten fresh
public Publisher calls over three invalid categories and deterministic repairs, and current
170-module App / 104-module host graphs with 22 shared managed modules. The reader and all ten
root tests pass under the exact hosted proof isolation envelope. It starts no listener, and it
never obtains workspace-write authority. Unsafe-symlink tests create their targets solely inside
runner-owned temporary directories while preserving rejection assertions.

The new product suites pass 21/21; the broader affected App run passes 141/141. Chromium passes
1/1 with screenshots inspected. Historical T01C/T04 roots pass 20/20 and guarded T05 roots pass
10/10. All 483 CI/retained-runner contract tests pass, including the 109-case checkpoint suite.
The exact six-command bounded local baseline passes. No exhaustive local gate is substituted for
G10's required later full audit, and none of these local results implies a hosted pass.

Checkpoint 76 retains all 75 prior entries and adds T06 without changing a frozen artifact. The
current head is `sha256:fc5bf1a8e02760b2cd02a9f8e781f28818a48f1492ebffc5741f5eb895ddf480`.
The raw checkpoint envelope derives from code-owned task generations plus exactly one candidate,
with separate finite ceilings. This repairs required append-test headroom without skipping tests,
changing canonical history, or granting live authority to the review candidate.

## Scope and remaining gate work

This proof covers the Web–React local product composition with the existing fixed publication and
activation destination. It does not claim corrupt revision recovery, Catalog mismatch recovery,
remote deployment, signing, native parity, P-12, N-036, or G10 closure. M10-T07 through T09 and the
full G10 audit remain separate requirements. Hosted exact-head PR and fresh main closure receipts
will be recorded only after their runs pass.
