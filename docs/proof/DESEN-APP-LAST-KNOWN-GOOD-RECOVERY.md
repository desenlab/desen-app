# Desen App last-known-good recovery

Task: M10-T07

Status: DONE

M10-T05 parent: `sha256:80c0b815a813ef462233b48a7fffe7c4d0bbf391aefc68eb9a6174da6bd84bd3`

M10-T06 parent: `sha256:1eb4260306d20fc87558edc4da4027c96bbb84598b758b242b8530010fe6071a`

M07-T08 parent: `sha256:c65d4f2de1407fffb891b5d3ba2fc8a3a8d4e3f0fb76c8b8f2719be6b310b3f9`

M07-T11 parent: `sha256:48bd9f85bd2da413fc72c1973a33732cc091796f9afc2863ec1eec15054314e0`

Final artifact: `sha256:5e589bc8022de3ccf3add7a9ebab78006ecca6e72628e165f54eef4fd8b90b68`

This is the implementation closure candidate, not hosted completion. The task remains active
until the bounded baseline, exact-head hosted Quality gate and Browser E2E, merge, and fresh main
checks pass. A pinned artifact authenticates evidence identity; it never substitutes for execution.

## Scope and authority

This task composes the existing public Publisher, immutable Bundle store, integrity, installed
package, reference, staging, activation, and recovery boundaries. A mutable channel remains
discovery intent, never active-runtime authority. Neither a corrupt candidate revision nor a
self-consistent Bundle with an incompatible Catalog digest may replace the last authenticated
delivery. No frozen protocol, Runtime Core, or historical proof artifact is changed.

## Independent execution layers

The sockets-free proof reader freshly executes the public API rejection and recovery matrix in
owned temporary storage. Closing and reopening that service proves service-level reconstruction;
it is not presented as a product-process restart. Complete current App and independent host graphs
are acquired freshly. Browser source declarations authenticate what must execute, not a stored
successful browser result.

The separate Chromium journey starts with normal visible authoring and publication. It then keeps
each rejected candidate selected in the channel while shutting down the complete service process,
waiting for its exit, and starting a different PID against the same private durable root. A fresh
browser context must recover the previous active revision with unchanged activation generation,
previous-good revision, and host static build. The normal App is rebuilt in the same temporary
output directory with its new launcher-owned activation origin; the public activation bridge
deliberately accepts only an OS-selected port. This changes App connection configuration, not its
source code or saved designer Source. App build lifetimes are recorded separately rather than
claimed byte-identical. The independent host is never rebuilt. A later valid publication must
activate normally.
The supervisor may construct only the explicitly named negative transport fixtures; it does not
inject a positive Source or create privileged product HTTP endpoints.

## Executed evidence

The final dedicated Chromium journey passes 1/1 in 16.1 seconds; two fresh repetitions of the
same final lifecycle behavior also pass. Visible normal-product authoring publishes A, then C.
Both a corrupt-revision candidate and a self-consistent Catalog mismatch are rejected before
activation. After each full shutdown and cold restart, the unchanged host recovers C with A as
previous-good and generation 1, even while the invalid candidate remains selected in the channel.
A final visibly authored D publication activates as generation 2 with C as previous-good. The
saved Source and App source fingerprint survive both restarts.

The sockets-free public API matrix independently passes both exact rejection stages, durable
reconstruction, and cloned, missing, or swapped-role authority negatives. Before importing public
built entrypoints, its reader compares 96 current emitted JavaScript modules against fresh in-memory
TypeScript emission and reauthenticates 442 compiler inputs. This is emission parity, not a replacement
for typechecking. The 304,094-byte artifact records 41 current tracked receipts, fresh unprojected
App/host build graphs, and browser declarations separately from actual browser execution.

An intentional timeout-only retention probe confirms that failure screenshots, videos, and action
timelines remain available, while the network trace is empty and source/configuration/credential
markers are absent. The probe remains a deliberately failed diagnostic, not a passing journey or a
retry. Owned child processes and temporary roots are cleaned after exit; no local product session
or user project is erased.

All 491 CI and retained-runner contract tests pass with zero skips or cancellations, including
110 checkpoint tests. The actual T07 verifier and all nine root cases also pass through the
authenticated default CI runner's exact Vite/SQLite permissions; build outputs remain unchanged.
All previous workloads are retained: 224 logical workloads / 107 proof pairs, 241 physical
workloads, eight independent Chromium journeys, two workers per shard, and eleven exclusive
barriers. The bounded baseline passes with 871 modules / 3,745 dependency edges and 38 boundary
fixtures. Checkpoint 77 preserves all 76 historical entries and 60 previous artifacts, reseals
only the four T05/T06 readers, and appends T07 at
`sha256:4bed8aba3916c066c689dd99ea4a3820e0b7b1a05d3780688e51b70ec9b97ece` (61 artifacts / 122 readers).

## Limits and required closure

The recovery contract authenticates both durable roles and does not implement automatic rollback.
Corruption of the durable active or previous-good Bundle itself must fail closed, not silently
select a different revision. Application-owned local storage is trusted; this is not hostile-admin,
anti-rollback, remote, multi-tenant, production-authentication, or native-platform evidence.
Ephemeral loopback credentials are excluded from retained network snapshots and sanitized proof
receipts; this does not close the repository-wide N-036 secret audit.

P-12 remains `NOT_PROVEN` until the fresh service matrix, actual new-process Chromium journey,
bounded local baseline, exact-head hosted checks, merge, and fresh main closure pass. T08 seed/reset,
T09 committed core baseline, and G10's consolidated milestone audit remain separate tasks.
