# M10A-T01 — Base UI adapter boundary

**Last verified:** 2026-09-10

**Status:** Local implementation and evidence pass; task remains `IN_PROGRESS` until exact-head
hosted CI passes. This report does not advance M10A-T02 or G10A.

## Delivered boundary

- Private package `@desen/starter-catalog-web` owns the finite Desen registrations
  `run.desen.starter/Button`, `run.desen.starter/Select`, and `run.desen.starter/Dialog` plus their
  Base UI React adapters and neutral CSS.
- The tracked [Catalog](../../packages/starter-catalog-web/catalog.json) is constructed through the
  public Catalog SDK, passes structural, semantic, and execution admission, and is self-bound to
  every emitted package byte, the package manifest and CSS, exact lock admission, the installed
  Base UI manifest, and the installed MIT license.
- Editor Core adds one atomic `insertDesenEditorSubtree` operation for required-slot defaults. The
  starter package, not Core, owns the component-specific subtree templates.
- `@desen/starter-catalog-web-proof` has separate authoring and host entry graphs. The authoring
  graph uses Publisher and Editor Core; the host graph does not. Both consume the same starter
  adapters and tracked Catalog.

No existing App workspace is migrated, no full component library is claimed, and Runtime Core is
unchanged.

## Exact package and dependency identity

The 40,745-byte Catalog is
`sha256:0641d4baea6967f57700363a4cdf47ada8118cf39fd7f3bc8a5ec46709402134`.
Its package digest is
`sha256:4a9c4029bade26ab398a47823cc8bec27c92c356dc77a73e1e902867f9051659`,
covering 17 compiled files and 23 digest entries.

The admitted production dependency is `@base-ui/react@1.8.0`, pinned in the lockfile at
`sha512-P0/1sxo6SBVZOklKMIedvTWqw2s2IQzi9x5bIVsXu980cuSOD4NeuRSs+/L7LZQfDkZP/uRZyGPyfFl/B1oH+Q==`.
The proof authenticates its eight transitive lock nodes, their exact snapshot dependency maps, the
React peer range, installed package manifest, and 1,072-byte MIT license
(`sha256:07fc1b39d69d14bc7d40482a628f47226258eb01265db68ae684944b916beb2a`).

The separately observed `pnpm audit --prod --json` result on 2026-09-10 was zero production
vulnerabilities. The verifier does not re-run that network-backed observation and the artifact
marks this explicitly. The all-dependency tree is not claimed clean: the existing development-only
`@vitest/mocker` / `vitest` moderate finding remains associated with
`GHSA-82fw-gwwq-j7x9`.

## Executed behavior

The focused Chromium and package suites cover:

- atomic required-slot insertion, Publisher-derived authoring, independent host graph, and a
  shared static adapter registry;
- keyboard Select behavior and JSON event projection;
- Dialog Escape, focus return and focus trapping, including nested portal containment;
- disabled/loading Button behavior, StrictMode unmount/remount, and compatible publication/live
  rerender identity;
- browser rejection of wrong event payloads, unknown capabilities, and unknown style parts; and
- package-level rejection of a portal target moved outside the approved boundary.

Within that combined set, the Chromium suite passes 3/3 and authenticates 16 exact assertions.

The custom Playwright reporter writes a duration-free receipt only after the three exact cases and
the full run pass. The live verifier refuses a stale receipt, owns `127.0.0.1:4187` exclusively,
uses runner-scoped OS temporary output, and compares the fresh result with the checkpointed
[13,910-byte artifact](artifacts/m10a-t01.json)
(`sha256:711f74398fb1d250d392dd4ff1145527cdaa7ca8673e811c7f753d211554cc74`).
Every live verification also launches a bounded POSIX process-group probe with a SIGTERM-ignoring
descendant on port 4187, forwards a real parent SIGTERM, escalates to the complete group, and proves
the listener can be rebound and closed afterward. A normal browser close is accepted only when its
owned group has no surviving descendants.

## Local verification

- Starter package: 30/30 focused tests pass.
- Editor Core: 152 tests across 11 files pass; the focused subtree and stable-ID set passes 24/24.
- Existing reference-catalog React adapter regressions: 10/10 pass.
- Browser proof: 3/3 Chromium cases pass.
- The isolated root proof passes 7/7; its fresh browser-exclusive verifier closes with code 0 and no
  signal under the real exhaustive runner environment.
- CI contracts pass: 49/49 orchestration, 38/38 shared-state, 44/44 required/exhaustive-equivalence,
  31/31 shard, and 115/115 reader-checkpoint cases.
- Checkpoint sequence 81 authenticates 65 artifacts and 130 current readers at
  `sha256:3c2a3290df150df591fa2149174a3514aa1c9e48644a2df49d148b55c1c478c2`, preserving sequences
  1–80 byte-for-byte.
- `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm boundaries` pass in the
  current local seal.

Primary commands are `pnpm generate:m10a-t01`, `pnpm verify:m10a-t01`, and
`pnpm test:m10a-t01`. The generator writes only the task-owned Catalog and evidence artifact. The
verifier rebuilds required package graphs, executes the real Chromium and cancellation proofs, and
rejects Catalog, artifact, dependency, reader, process-group, output, or observation drift.

## Remaining closure condition

M10A-T01 stays `IN_PROGRESS` until the current exact head passes hosted required CI. Local evidence
does not prove package publication, remote or multi-user hosting, production authentication,
additional starter components, theme/token work, an App migration, M10A-T02, or G10A.
