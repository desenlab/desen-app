# M10A-T09 — Data-display and feedback starter capabilities

**Status:** `IN_PROGRESS` — local evidence is captured; exact-head hosted Quality and fresh-`main`
closure remain required before this task can be `DONE`.

## Delivered local boundary

T09 advances the private `@desen/starter-catalog-web` Catalog to
`run.desen.starter.web@0.6.0#web-react`, retaining the preceding starter families and adding Card,
Badge, Avatar, Alert, List, basic Table, Skeleton, and Progress. List has an explicit item slot;
List and Table take bounded inert sample data with stable item or row identifiers. The Table renders
native caption, header-cell, and data-cell semantics. Avatar requires a text alternative. Examples
cover empty, loading, error, and ready states without binding an operation.

The boundary deliberately excludes enterprise-grid behavior, charts, server pagination, sorting,
callbacks, renderers, selectors, executable markup, remote media, normal-App integration,
persistence, Publisher or Runtime authority, Core/protocol changes, G10A, and M11.

## Local evidence

The task-owned artifact is [`artifacts/m10a-t09.json`](artifacts/m10a-t09.json): 5,584 bytes at
`sha256:7fecf6a1b5eebb4a132f55e9641b6137b772bd1bb0df7fb380ef9c1f68289d09`. It binds the 0.6.0
Catalog (585,861 bytes at
`sha256:4e59de0135e7d445bc8c6e029506369bf8b38e40d5df5a57a39f81a799a42a69`) and public package digest
`sha256:88a1fac65d43d166cbc31dceb890468950f677395ab93a62a382955e16b0c709`.

Reader checkpoint sequence 100 preserves earlier receipts and authenticates 73 artifacts and 146
readers at `sha256:76591ae46cf96d4800ca664b1669d942f64f92e32a642a9796d6f86a7a1f771a`; T08 remains its
historical 0.5.0 receipt while T09 owns the current Catalog boundary.

The isolated Chromium proof passes four authoring/independent-host cases. It observes all added
capabilities, text alternatives, empty/loading/error/ready examples, the native Table structure,
bounded repeat and stable row identity, and the same static adapter registry in both graphs. It
also rejects malformed data and unadmitted grid authority. The root proof mutates artifact bytes,
observations, and unsafe options.

```bash
pnpm --filter @desen/starter-catalog-web test
pnpm --filter @desen/starter-catalog-web test:public-package
pnpm --filter @desen/starter-catalog-web-proof test:m10a-t09
pnpm generate:m10a-t09
pnpm verify:m10a-t09
pnpm test:m10a-t09
```

## Closure still required

This document records only local, reproducible evidence. T09 becomes `DONE` only after the exact
PR head passes the hosted required graph, that exact head merges, and a fresh hosted `main` run at
the merge SHA passes. Those receipts will be appended here rather than inferred from local output.
