# M10A-T17 — Design-system explorer and documentation

**Status:** `DONE` — exact-head hosted, squash-merge and fresh-`main` closure are recorded below.
Last reviewed: 2026-09-20.

## Hosted implementation receipt

[PR #135](https://github.com/desenlab/desen-app/pull/135) passed
[run 35498174487](https://github.com/desenlab/desen-app/actions/runs/35498174487) at exact head
`4e3cce4`. Browser E2E, exhaustive proof A/B/C, routing and the joined Quality gate all passed.
The PR squash-merged into `main` as
[`b425675c7f1a517eec95ce19a8475aa22386f187`](https://github.com/desenlab/desen-app/commit/b425675c7f1a517eec95ce19a8475aa22386f187).
The post-merge [fresh-main run](https://github.com/desenlab/desen-app/actions/runs/35499581683)
passed Browser E2E, exhaustive proof A/B/C, routing and the joined Quality gate at the merge SHA.
Earlier failed candidates remain failures; this receipt is the T17 closure authority.

## Delivered boundary

T17 adds an App-owned Design System area generated from the admitted Catalog and current Source.
It provides a searchable component gallery, deterministic foundation scales, usage guidance,
schema-derived props, slots, style parts, declared events/commands, visual states and inert
scenario examples. The area is a documentation and authoring aid; it does not create a second
component registry or a handwritten Storybook story tree.

Catalog and Source mismatches fail closed and are visible to the user. Missing runtime adapters
are reported as `missing`; documentation remains data-only and never executes Catalog-provided
scripts, callbacks or components. Scenario props are transient display data and do not mutate the
authored Source. Existing Design, Run, persistence and frozen protocol boundaries remain intact.

## Implementation evidence

- `apps/desen-app/src/design-system-explorer.ts` projects the bounded Catalog/Source contract into
  immutable, searchable documentation and foundation-token models.
- `apps/desen-app/src/application.tsx` and `application.module.css` mount the Design System area
  beside the existing project surface without mounting a Source editor for documentation alone.
- `apps/desen-app/src/project-navigation.ts` adds the project-scoped route.
- `apps/desen-app/test/design-system-explorer.test.ts` covers deterministic projection, visible
  adapter mismatch and frozen foundation data; the application suite covers navigation and the
  mounted user-facing area.

## Explicit non-claims

T17 does not add library release/adoption management, Connections, behavior wiring, Publisher or
Runtime activation, arbitrary code execution, multiplayer editing, G10A or M11 authority. Those
remain owned by later M10A tasks and the frozen protocol/Core boundaries.
