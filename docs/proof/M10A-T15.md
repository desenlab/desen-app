# M10A-T15 — Project-local masters and instances

**Status:** `DONE` — exact-head hosted, squash-merge and fresh-`main` closure are recorded below.
Last reviewed: 2026-09-19.

## Hosted implementation receipt

[PR #131](https://github.com/desenlab/desen-app/pull/131) passed
[run 35454344114](https://github.com/desenlab/desen-app/actions/runs/35454344114) at exact head
`67d358a04f783e136c65a488f70aafb3838f40ac`: Browser E2E, all three exhaustive shards and the
joined Quality gate passed. The T15 verifier executed all 17 fresh workloads against the current
artifact, followed by all 15 root tests. C observed 36/36 successful closes under the reviewed
partition; no skipped rollback job is counted as proof.

The PR squash-merged as `577c15235e0cef8eaf7af2c8bb603b2e8d6d5dcd`, whose tree matches the
proved head exactly. Its [fresh-main run](https://github.com/desenlab/desen-app/actions/runs/35456101864)
passed Browser E2E, exhaustive A/B/C, routing and the joined Quality gate at that merge SHA.
Earlier failed candidates remain failures; this receipt is the T15 closure authority.

C's successful runner took 1,580,935 ms against the unchanged 1,620,000 ms deadline. This repairs
the measured distribution failure, not a claim of a large performance margin or general speedup.

## Delivered authoring boundary

The version-2 editable-project record adds an explicit bounded recipe graph. Version 1 imports
losslessly with an empty graph; its old inert recipe metadata is never reinterpreted as executable
definitions. The public platform-neutral Core API admits definitions, nested occurrences, stable
identity mappings and explicit overrides. Whole-graph admission includes unused definitions and
requires managed regions to materialize to the stored ordinary DESEN Source exactly.

Capture, create/update/delete, insert, override/reset/detach and ordinary Source reconciliation
are immutable whole-project transactions. Expected project and transitive definition digests
reject stale work. Two-pass qualified identities retain conceptual nodes and wiring across
nested composition; only schema-owned references are remapped, while literal strings and opaque
extensions remain unchanged. Recursive definitions, conflicting structure, cross-occurrence
binding borrowing, unknown capabilities and finite-limit overflow reject without partial state.
Detach removes metadata without changing Source; fresh-ID copies are explicitly detached.

The normal DESEN Neutral App uses one aggregate controller for Source, recipe metadata, history,
preview, dirty state and generation-fenced persistence. Its existing Components pane offers
My components, capture/insert, reset/detach and Edit master. Master editing uses the SAME existing
Inspector, Style and composition tools, with a small Apply/Discard banner. The isolated draft
has its own undo/redo and cannot Save, Open, navigate or Run/Publish. Apply validates the eventual
complete live project and records one live transaction; Discard preserves that project.

The lifecycle rejects incoherent generations and stale/reentrant/disposed completion. An uncertain
write cannot be cleared by discard or retried as a clean save. Existing Account/Flow Source-only
profiles retain their old authority; Publisher and Runtime never interpret the recipe graph.

## Fresh evidence ownership

`scripts/lib/m10a-t15-workloads.mjs` owns 17 serial workloads: App/control-plane builds, frozen
Protocol/Runtime checks, three built public-package contracts, Core/theme/starter/asset/history/
App tests, browser types, four isolated workbench journeys, the existing T12 styling journey and
the new normal-product T15 journey. Build writers and fixed-port browser work never overlap.

The executor reads actual Vitest and browser reports. It rejects failed, skipped, pending,
missing, duplicated or foreign required suites, every lost T14 reuse case, missing original
theme/style assertions, an invalid workbench graph and incoherent master save/reopen receipts.
A zero child exit without its required observation cannot authorize success. Production capture
and verification accept no caller-supplied observations, workspace override or injected runner.

The Chromium journey captures a master and three instances through visible controls. It preserves
one local text override, detaches the third, edits master text and color in the existing tools,
updates both linked instances, undoes the apply as one project edit, redoes, saves and reloads.
Observed successful workspace PUT/GET records must match the complete graph, mappings, overrides
and Source; unchanged fingerprints or an aliased detached instance reject. Draft controls issue
no workspace PUT. Credentials, response headers and trace snapshots are not evidence content.

The local pre-capture run passed 570 unit/integration assertions (Core 158, theme 32, starter 84,
assets 11, Source history 19 and App 266), all three public contracts and all six browser cases.
The proof contract has 15 positive/negative root tests. These counts describe local observations,
not successful hosted execution. The final artifact is generated only after a fresh complete run
and equal before/after receipts for every owned source input.

The first local capture bound 184 source inputs in the 128,043-byte
`docs/proof/artifacts/m10a-t15.json`, SHA-256
`28ffdd34a9c91ebc907b1acdd29afcb8984acc4463bba0106f9703a6b888dcc7`.
All 17 capture workloads and the independent fresh verifier passed for that first candidate.
The browser observes the complete actual HTTP records without importing private App source types.
The unchanged dependency policy passes across 1,169 modules / 4,865 edges and its negative fixtures.
The exact bounded local baseline, all CI-policy test files plus the root quality-gate contract,
and all 34 T02/T03/T12/T13/T14/T15 root reader tests passed. Reviewed checkpoint 140 preserves
the committed 139-record prefix and all 76 predecessor artifacts, admitting the T15 pair at
`a1be3e89d95566d360b8795076b6eeca0f7df590e898258c0f570f7e5c9b1056` (77 artifacts / 154 readers).

### First hosted candidate and correction boundary

The first candidate, `e64c09d024672201cb11560cba3a853ccac5c8d9`, failed
[PR #131's run](https://github.com/desenlab/desen-app/actions/runs/35446546547).
It is not closure evidence. Browser verification exposed the missing T15 handover in older live
graph readers; package aggregation exposed four real-Catalog integration timeouts. The capture
above belongs to that earlier candidate and does not authenticate subsequent corrections.

The correction retains all frozen M09/M10 artifacts and authenticates the complete current
78-file App inventory, its 73 reachable sources, the freshly built 708-module App graph, and the
unchanged independent host. Exact current/predecessor receipts bound the historical input
projections; changed, truncated, stale or replayed source is rejected. Historical graph values
are separate from current observations. New shared bridge data belongs to T15's source capture
and conservative exhaustive CI ownership, not an untracked compatibility exception.

An isolated CPU profile of the linked-instance controller journey took 17.72s in its test body;
schema validation and canonicalization dominated sampled CPU work. Hosted runs reached 50.7s
for a controller integration and 225.9s for the full Neutral UI journey. Only the three affected
controller cases receive 60s budgets and that UI journey receives 300s. All real Catalog,
Publisher, history, persistence and UI assertions remain; neither validation caching nor a
global timeout increase is introduced. These test observations are not browser performance claims.

The corrected capture and independent fresh verifier each passed all 17 workloads. The current
artifact binds 185 source inputs in 128,235 bytes, SHA-256
`7ef3ff271f466b34de9ce0b3f406367b41e52a2aec3880db34663a0baae5d0ae`.
Its claims, required workloads and observed execution results match the first capture; the new
input is the exact legacy-receipt bridge. No prior owned input or assertion is removed.
Reviewed checkpoint 141 preserves all 140 predecessor records and all 76 predecessor artifacts,
resealing only the eleven corrected readers and T15 artifact at
`9f41ddc718c989a06811b4ed016f1d4c28c3d6288f8722192a3e22b93767b8d9` (77 artifacts / 154 readers).
Live checkpoint verification and the exact six-command bounded local baseline pass. The complete
637 CI-policy/quality-gate contract tests and all 34 T02/T03/T12/T13/T14/T15 root reader tests pass,
with no skipped tests. CI ownership admits exactly 1,912 paths, retaining every pre-T15 path;
the 260-workload inventory, 123 proof pairs and 285 physical shard workloads are unchanged.
These are local receipts only. An exact-head hosted run, merge, fresh-main verification and
separate proof/status closure remain required before this correction can close T15.

## Historical handover

### Persistence-reader correction

Candidate `97f0647996b5f869009ce04ac3d1a53d1e833464` failed
[run 35450281491](https://github.com/desenlab/desen-app/actions/runs/35450281491).
Browser E2E and exhaustive shards A/B passed; shard C exposed six M09-T12 negative tests using
current persistence controls in place of their historical baseline. That failure reproduced
locally (11/17 passing). It remains failed evidence, not closure.

The historical reader now obtains that one missing file through the already authenticated T15
inverse projection and verifies the original M09-T12 9,272-byte SHA-256 receipt. It still applies
caller mutations last and returns detached copies. The regression checks exact historical bytes,
separation from current bytes, mutation precedence and copy isolation; the persistence policy
test now first requires its unmodified baseline to pass. No source-policy condition is weakened,
and neither production code nor any of the 77 artifact files changes in this correction.
The three directly affected root suites pass all 44 tests locally.

Checkpoint 142 preserves every previous record and artifact, advancing only the persistence
root test and the evergreen historical-reader pair to
`58f6aab69cf3be5256dd4a81ffbf3b0d3f2c4f15417c7adef4a2d2bd39d222ac`
(77 artifacts / 154 readers). This is current reader identity, not cached execution success.
The final checkpoint passes all 638 CI-policy/quality-gate contract tests, all 34
T02/T03/T12/T13/T14/T15 root tests, the 27 recovery/diagnostics tests and the exact bounded local
baseline. All 185 T15-owned source receipts still match the existing capture; no fresh artifact
is inferred from these reader tests. Exact-head hosted and fresh-main closure remain required.

### Shard deadline correction

Candidate `0600965514dd6f5cc3ea080db84b23ebcc676bd7` failed
[run 35451987190](https://github.com/desenlab/desen-app/actions/runs/35451987190).
Browser E2E passed in 10m39s, exhaustive A in 15m59s and B in 18m22s. C's runner exhausted its
unchanged 1,620,000 ms deadline: 86/88 closes were observed, the T15 verifier was cancelled after
181,776 ms and its root test was not run. T15 began at 15:55:02 UTC; timeout arrived at 15:58:04.
Its `app-behavior` failure report coincided with cancellation; no failed assertion is identified.
The earlier persistence regression passed in this hosted run. Neither failure becomes closure.

The fixed shard partition now preserves all 123 pairs in their original global order while moving
the contiguous boundaries to 81/30/12. All 260 logical and 285 physical workloads, the full repeated
prefix, 11/0/4 exclusive barriers, two workers, 27-minute deadline, process isolation and closing
guards remain unchanged. No T15-owned source input, task artifact, checkpoint reader or workflow changes
by this scheduling correction. The [ADR](../adr/0018-fresh-proof-performance.md#t15-extension--rebalance-the-unchanged-exhaustive-inventory)
records its boundary; fresh hosted execution remains required.

The correction passes all 639 CI-policy/quality-contract tests, all 34 predecessor/T15 root
reader tests and the six-command bounded local baseline. Independent checks retain all 185
captured T15 inputs, the artifact, checkpoint 142 and the frozen Runtime Core tree unchanged.
The old partition is rejected even when a receipt names the same run, revision and total coverage.
Local Turbo cache hits are early feedback, not fresh hosted or renewed T15 behavior evidence.

### Completed predecessor boundaries

T02, T03, T12, T13 and T14 artifact bytes remain immutable. Their current readers authenticate
their exact historical identity and checkpoint; they do not relabel an old result as a new
implementation observation. Their old generators reject recapture. Fresh behavior belongs to
T15: complete Core/token/authoring coverage, the original workbench assertions and isolation graph,
the exact 0.7.0 Catalog and T12 styling journey, asset admission, Source history and all seven
original App reuse cases. The normal Browser E2E chain retains every previous journey and appends
T15. Historical hosted receipts in the predecessor documents retain their original meaning.

## Reproduction and closure boundary

The reviewed CI ownership table covers all 1,912 tracked paths: all 1,867 pre-T15 paths remain,
with exactly 45 additions and 246 verifier/root-test paths. Its ordered path-set digest is
`4d298307879bb63be0afbc3a2e63e4ec3a72a867bdf24059ce5f6be76ec086e3` and ownership digest is
`553b7498aa5862b92b7e7c7831261f64fb650d4bb0fd5b00cf24360e2fcca75f`. All 37 ownership tests
pass. Every planned closure document remains `FORCE_EXHAUSTIVE`; this receipt authenticates
coverage and ownership, never cached test success. The 260 logical workloads, 123 proof pairs,
285 physical workloads and all 15 exclusive barriers remain required.

```bash
pnpm test:m10a-t15
pnpm generate:m10a-t15
pnpm verify:m10a-t15
```

Generation writes only the T15 artifact. Verification authenticates current source receipts,
executes fresh work and compares the complete canonical evidence; it does not reuse a prior PASS.
The bounded local baseline, reviewed reader checkpoint, complete CI ownership, exact-head hosted
Quality gate, merge and fresh-main closure are complete. T15 remains bounded to local authoring
and persistence; it adds no variants, library release/adoption UI, Connections, Runtime macro,
protocol field, arbitrary CSS/code execution, deployment, G10A or M11 authority.

T15 adds no variants, library release/adoption UI, Connections workspace, multiplayer editing,
Runtime macro, protocol field, arbitrary CSS/code execution, production deployment, G10A or M11
authority. The browser proves authoring and local persistence, not master publication/activation.
