# Changesets

Changesets describe intentional public API and package changes. No package is publishable during
the proof phase. Release automation remains disabled until the public-alpha release gate passes.
